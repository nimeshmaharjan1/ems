/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['naajvhwyqrtaxybvcdzm.supabase.co', 'images.unsplash.com'],
  },
};

module.exports = nextConfig;
